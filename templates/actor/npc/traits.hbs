<div class="edo-tab-content">

    {{!-- Core Stats --}}
    {{> 'newedo.traits-core'}}
    <div class="flexrow flex-gap-m flex-group-start align-start scrollable">
        <div>
            <a data-action="configSkills">
                <i class="fas fa-cog"></i>
                Configure Skills
            </a>
            <ol class="edo-skills-list items-list">
                {{#each items.contents as |item|}}
                    {{#if (eq item.type 'skill')}}
                        <li class="item flexrow" data-item-uuid="{{item.uuid}}">
                            <a data-action="use">{{item.name}}</a>
                            <div class="flexrow">
                                {{#each item.system.ranks as | value index |}}
                                    <button class="edo-skill-dice-button" data-action="skillDice" data-index="{{index}}" value="{{value}}">
                                        {{#if (not (eq value 0))}}
                                            {{value}}
                                        {{/if}}
                                    </button>
                                {{/each}}
                            </div>
                        </li>
                    {{/if}}
                {{/each}}
            </ol>
        </div>
        <div>
            <ol class="items-list">
                {{#each items.contents as |item|}}
                    {{#if (not (eq item.type 'skill'))}}
                        <li class="item flexrow" data-item-uuid="{{item.uuid}}">
                            <span class="flexrow align-center flex-start">
                                <img src="{{item.img}}" width="30" height="30">
                                <a style="margin-left: 4px;" data-action="edit">{{item.name}}</a>
                            </span>
                            <div class="item-controls">
                                {{#each item.system.actions as | button |}}
                                    {{#if button.condition}}
                                        <a data-action="use" data-use="{{button.action}}" class="flexrow"><i class="{{button.icon}}"></i></a>
                                    {{/if}}
                                {{/each}}
                                <a data-action="edit"><i class="fas fa-edit"></i></a>
                                {{#if ../isEditMode}}<a data-action="delete" title="Delete"><i class="fa-solid fa-trash"></i></a>{{/if}}
                            </div>
                        </li>
                    {{/if}}
                {{/each}}
            </ol>
        </div>
    </div>
</div>